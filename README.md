# acil_durum_takip (backup)

This repository is a backup mirror of the `acil_durum_takip` Flutter project.

Notes:
- This copy contains files copied for safekeeping; it may include intermediate
  or local-only files.
- The primary project is `acil_durum_takip` — use this repository as a backup.

## Project summary

`acil_durum_takip` is a Flutter application for tracking emergency bags.

The backup repository here was created to store a mirror of the main project
and developer artifacts. It may contain additional local files and history.

## Getting Started

This is a Flutter application. If you want to run it locally, make sure you have
the Flutter SDK installed and follow the standard Flutter setup:

- https://docs.flutter.dev/get-started/install

Once Flutter is set up, run:

```bash
flutter pub get
flutter run
```

For more details and Flutter resources, see the official docs.
